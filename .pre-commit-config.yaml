repos:
  - repo: https://github.com/prettier/prettier
    rev: 2.1.1
    hooks:
      - id: prettier
        # TODO: might be able to remove redundant args: https://github.com/prettier/pre-commit/issues/22
        args: [--write, --list-different, --ignore-unknown, --single-quote=true]
